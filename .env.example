N8N_USER=admin
N8N_PASSWORD=ChangeThisToAStrongPassword

# Optional but strongly recommended: encryption key for stored credentials
# Generate with: openssl rand -base64 32
# IMPORTANT: if you change this after storing credentials, they become unreadable.
N8N_ENCRYPTION_KEY=<generate_and_put_here>

# If you later expose your instance to the internet, set WEBHOOK_URL to your public URL
#WEBHOOK_URL=https://example.com